# Pattern Naming Rules

Display name (YAML `pattern:`): 1–100 chars, charset `[a-zA-Z0-9 _-]`, spaces allowed.

Filename (derived): spaces → `-`, lowercased, `.md` extension. Deterministic — do not randomize/hash/timestamp.
